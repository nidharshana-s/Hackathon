# PostgreSQL
PORT=4000
PGHOST=localhost
PGPORT=5432
PGDATABASE=hackathon
PGUSER=postgres
PGPASSWORD=

# Groq (server-side only; never expose this value in the frontend)
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
